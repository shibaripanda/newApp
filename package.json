{
  "name": "new",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "app": "concurrently \"(npm run start --prefix client)\" \"(npm run start:dev --prefix server)\"",
    "upnpm": "npm i && cd server && npm i && cd .. && cd client && npm i && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
